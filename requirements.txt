# MangaArr — dépendances Python
flask>=2.3
requests>=2.31
beautifulsoup4>=4.12
pandas>=1.5
Pillow>=9.0        # compression WebP des covers (image_cache.py)

# Outils système (apt/pacman) :
# CBR → CBZ  : unrar-free  (ou p7zip-full)
# PDF → CBZ  : poppler-utils (pdftoppm)
# WebP alt   : webp (cwebp) — fallback si Pillow absent
